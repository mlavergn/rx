import { ReplaySubject } from 'rxjs/ReplaySubject';
import { retryWhen, repeatWhen, delay, mergeMap } from 'rxjs/operators';

/**
 *
 */
export class MergeMap {
    static demo() {
        console.log('hello');
    }
  }
